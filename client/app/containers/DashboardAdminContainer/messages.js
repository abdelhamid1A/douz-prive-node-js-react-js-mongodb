/*
 * DashboardAdminContainer Messages
 *
 * This contains all the text for the DashboardAdminContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DashboardAdminContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DashboardAdminContainer container!',
  },
});
