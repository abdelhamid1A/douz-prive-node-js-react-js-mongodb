// import { selectDisplayItemPageDomain } from '../selectors';

describe('selectDisplayItemPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
