/*
 * ValidationAccountPage Messages
 *
 * This contains all the text for the ValidationAccountPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ValidationAccountPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ValidationAccountPage container!',
  },
});
