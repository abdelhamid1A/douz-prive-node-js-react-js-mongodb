// import { selectValidationAccountPageDomain } from '../selectors';

describe('selectValidationAccountPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
